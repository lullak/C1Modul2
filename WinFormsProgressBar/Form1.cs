namespace WinFormsProgressBar
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }

        private void progressBar1_Click(object sender, EventArgs e)
        {
            DateTime dateSeconds = DateTime.Now;
            DateTime dateMinutes = DateTime.Now;
            DateTime dateHours = DateTime.Now;

            progressBar1 = new ProgressBar();
            progressBar1.
            progressBar1.Show();

        }
    }
}
